//24. Write out an object literal to represent the data below.

const mySchool = {
  school: 'Guvi',
  className: 'Geek',
  schoolNumber: 6,
  incubatedby: 'IIT-M',
  lane: 'RP',
  city: 'Chennai',
};

console.log(
  `${mySchool.school},${mySchool.className},${mySchool.schoolNumber},${mySchool.incubatedby},${mySchool.lane},${mySchool.city}`
);
